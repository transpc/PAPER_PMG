# 루프 엔지니어링 프로토콜 (LOOP.md)

> 작성일: 2026-08-09
> 상위 문서: [PLAN.md](PLAN.md) — 전체 계획. 이 문서는 그 계획을 **실행하는 방식**을 정의한다.
> 기록 파일: [LOG.md](LOG.md) — 모든 사이클의 결과가 append 된다.

---

## 1. 개념과 원칙

루프 엔지니어링 = PLAN.md 의 각 Phase 를 **작고 검증 가능한 사이클**로 쪼개어 반복 실행하는 것.

```
┌─> 1 목표 선언 (1문장, 검증 가능한 형태)
│   2 변경 (커밋 가능한 최소 단위)
│   3 실행 (build / test / run — 반드시 in_contain.sh 경유)
│   4 판정 (아래 §2 판정 체계, L1→L2→L3)
│   5 LOG 기록 (§4 규약 — 성공/실패 불문)
└── 6 green → 커밋 후 다음 사이클 / red → §5 실패 처리
```

**원칙**
- **한 사이클 = 하나의 검증 가능한 변화.** 두 가지를 한 번에 바꾸지 않는다 (red 시 원인 분리 불가).
- **모든 사이클은 LOG.md 에 기록된다.** 실패한 사이클일수록 기록 가치가 크다.
- **green 상태에서만 전진.** red 를 안고 다음 사이클로 넘어가지 않는다.
- **냉정 기록.** 결과는 관측된 사실(출력, 수치)만 적는다. 해석·추측은 `추정:` 접두어로 사실과 분리한다. 기대와 다른 결과를 미화하지 않는다.

---

## 2. 판정 체계

### 핵심 판단 (사용자 결정, 2026-08-09)

> 압력 수정 방정식과 소스(RHS)가 CSR 로 뽑히면 residual 로 작동 여부를 확인할 수 있다.
> 따라서 **수렴에 필요한 반복 횟수(its)를 1차 지표로 작동/오작동을 판정**한다.

CSR 덤프 `(A, b)` 만 있으면 어떤 변경 후에도 `‖b − A·u‖ / ‖b‖` 를 독립적으로 계산해 솔버가 실제로 방정식을 풀었는지 확인할 수 있다 (솔버 자기 보고 잔차에 의존하지 않음). 여기에 its 를 붙이면 "풀긴 푸는데 느려졌다(성능 회귀)"까지 잡힌다. PLAN §4-4 의 bitwise/해 벡터 비교는 이보다 상위의 **마일스톤 검증**으로 격하하고, 매 사이클 판정은 its/residual 로 가볍고 빠르게 돈다.

### 판정 레벨

| 레벨 | 시점 | 내용 | 기준 |
|------|------|------|------|
| **L1 스모크** | 매 사이클 | 빌드 성공, 실행이 비정상 종료 없이 완료 | 컴파일 에러 0, crash/hang 없음 |
| **L2 기능** (1차 판정) | 솔버를 건드린 모든 사이클 | ① 독립 계산 residual `‖b−Au‖/‖b‖ ≤ crit` 도달 ② its 를 ref 와 비교 | 아래 판정표 |
| **L3 골든** | 마일스톤 (Phase 완료, 태그 전) | 해 벡터 비교 — PLAN §4-4 기준 (리팩터링이면 bitwise) | PLAN §4-4 표 |

### L2 판정표

| 판정 | 조건 | 조치 |
|------|------|------|
| **PASS** | 수렴 도달 **and** its == ref (리팩터링) 또는 its ≤ ref (개선 의도 변경) | LOG 기록 → 커밋 |
| **WARN** | 수렴은 하나 its > ref | LOG 에 its 증가량 기록 → 원인 분석 후 진행/롤백 판단 (묵살 금지) |
| **FAIL** | 발산, 정체(maxit 도달), NaN/Inf, 독립 residual 이 crit 초과 | §5 실패 처리 |

### ref_its 기준표

레퍼런스 확보 시점에 아래 표를 채우고, 이후 모든 L2 판정의 기준으로 삼는다. 갱신은 "의도적 알고리즘 개선" 사이클에서만 허용하며 갱신 사유를 LOG 에 남긴다.

| 문제 | rank | 시점/조건 | ref_its | 최종 residual | 채취 사이클 |
|------|------|-----------|---------|---------------|-------------|
| 합성 Poisson 등방 24³ (13,824셀) | 1 | crit=1e-8 | **4** | 3.97e-9 (독립 계산 일치) | C007 |
| 합성 Poisson 이방 24³ (aspect=100) | 1 | crit=1e-8 | **4** | 1.28e-10 | C007 |
| 합성 Poisson 등방 48³ (110,592셀) | 1 | crit=1e-8 | **5** | 1.63e-10 | C007 |
| iSMR ECT 436K셀 (rv_model=0, **구입력** blob d145c81) | 4 | 초기 과도 step 1~38 | **1~3 (최빈 2)** | ≤ 8.9e-9 | C005-r8 (fort.501, 76솔브) |
| iSMR ECT 436K셀 (**ECT1 신입력** blob 29ee238, d5e2177) | 4 | 초기 과도 step 1~48 | **1~7 (최빈 2)** | ≤ 9.7e-9 | C016 (fort.501, 97솔브) |
| **골든 재생** iSMR 436K (하네스 기준, 구입력 덤프) | 1 | s1/s10: its **2,2** · s30: **3,3** | 좌동 | 충실도 ≤1e-10 (gate 1e-9), 베이스라인 bitwise | C009 (golden/iSMR436k_np1/meta.md) |
| **골든 재생** ECT1 436K (하네스 기준, 신입력 덤프) | 1 | s1/s10: its **1,1** · s30: **3,3** · s150(정착): **9,9** | 좌동 | 충실도 ≤2.4e-7=k1 (gate 1e-6), 베이스라인 bitwise | C017, 재베이스라인 C011-3r1 (golden/ECT1_436k_np1/meta.md) |

> 입력 이원화 (2026-08-12, 상류 d5e2177): 케이스 실측 기준은 입력 버전에 결박된다. 골든 덤프·C005 스모크 기준은 **구입력**(복원: `git show d5e2177^:code/2_iSMR_ECT_res1/somaFlow.in`), 현 워크트리 입력은 **ECT1 신입력**(C016/C017 행 기준). 두 골든 세트는 run_tests.sh 병행 게이트. 구입력의 step 38 발산은 신입력에서 미재현. max_bicg 는 10000→1000 으로 줄었으나 관측 최대 its 7 로 조기정지 없음 (C016).

---

## 3. 사이클 절차 상세

1. **목표 선언**: "무엇이 어떻게 되면 이 사이클은 성공인가"를 1문장으로. LOG 엔트리의 `목표:` 란에 먼저 쓴다.
2. **변경**: 파일 단위로 최소화. 소스 변경 사이클과 스크립트/인프라 사이클을 섞지 않는다.
3. **실행**: 해당하는 것만 —
   - 인프라 사이클: 스크립트 직접 실행 + DoD 체크 (PLAN §2-3, §3-4)
   - 빌드 사이클: `scripts/build.sh`
   - 솔버 사이클: `pmg_standalone/run_tests.sh` (L2 지표 출력 포함)
   - 케이스 사이클: `scripts/run.sh`
4. **판정**: L1 → (해당 시) L2 → (마일스톤 시) L3. 상위 레벨 실패 시 하위가 green 이어도 red.
5. **LOG 기록**: §4 형식. 실행 명령과 핵심 출력(수치)을 그대로 옮긴다.
6. **마무리**: green → `/git_commit` 으로 커밋. red → §5.

---

## 4. LOG 규약 (LOG.md)

- **append-only.** 과거 엔트리는 수정하지 않는다 (오기 정정은 새 줄에 `정정:` 으로).
- 사이클 번호 `C###` 는 단조 증가. 실패 후 재시도는 `C###-r1`, `C###-r2`.
- 엔트리 템플릿:

```markdown
## C### | YYYY-MM-DD | <제목>
- 목표: <성공 조건 1문장>
- 변경: <파일 목록 또는 커밋 해시>
- 실행: <명령어 그대로>
- 결과: <관측 사실만 — 출력 요약, its/residual 수치, 소요 시간>
- 판정: PASS | WARN | FAIL — <근거 (레벨별)>
- 다음: <다음 사이클 또는 남은 문제>
```

- L2 해당 사이클은 `결과:` 에 반드시 표 형태로:

```markdown
| 문제 | rank | its (ref) | 독립 residual | 판정 |
|------|------|-----------|---------------|------|
| poisson_iso | 1 | 12 (12) | 3.2e-11 | PASS |
```

---

## 5. 실패 처리

1. **FAIL 발생** → 같은 번호로 `-r1` 사이클 개시. 목표는 "원인 규명" 자체로 바꾼다.
2. **원인 규명 수단** (순서대로): 변경 diff 재검토 → 이분 탐색(변경 반으로 쪼개 재적용) → `DEBUG= -g -traceback` + `CHECK=` 옵션 빌드 (makefile.in 의 주석 처리된 체크 옵션 활용: `-fpe0`, `-check bounds` 등)
3. **2회 재시도에도 원인 불명** → 롤백(`git checkout` / `revert`)하고 FAIL 상태 그대로 LOG 에 남긴 뒤, 접근 방식을 바꾼 새 사이클 번호로 재계획.
4. 롤백도 사이클이다 — LOG 에 기록한다.

---

## 6. 사이클 백로그 (PLAN → 사이클 매핑)

상태: ☐ 대기 / ▶ 진행 / ✔ 완료 / ✖ 중단 — 완료 시 LOG 엔트리 번호를 링크.

| # | 사이클 | PLAN 근거 | 선행 | 상태 |
|---|--------|-----------|------|------|
| C001 | `env.sh` + `in_contain.sh` 작성, SIF 스모크 (mpiifort 버전, `$METIS_LIB` 접근) | §2-2, §2-3 | — | ✔ [LOG C001](LOG.md) |
| C002 | `makefile.in.apptainer` (AVX2, p=12, METIS_LIB/rpath) + `build.sh` → `cupid.x` 링크 성공 | §3-1 | C001 | ✔ [LOG C002](LOG.md) (r1) |
| C003 | `.gitignore` 정비 + `prepare_case.sh` (호스트 7z 해제) | §3-1(5), §3-2 | — | ✔ [LOG C003](LOG.md) |
| C004 | `cupid.x` 단독 실행 → 입력 단계 도달 확인 (관측: 부재 가드는 죽은 코드, EOF 런타임 에러로 종료 — LOG 참조) | §3-1(3) | C002 | ✔ [LOG C004](LOG.md) |
| C005 | `run.sh` 작성 + iSMR 케이스 스모크 (수 timestep, PMG 경로 진입 로그) | §3-3, §3-4 | C004 | ✔ **PASS(스모크 기준)** — 38스텝·PMG 76솔브(its 1~3). 완주는 rv_model=0 물리 발산으로 미달 (rv_model 은 고려하지 않기로 결정, 2026-08-10 — 스모크 결과로 충분) [LOG C005-r1~r8](LOG.md) |
| C006 | `pmg_standalone/` 의존성 클로저 + GMG 단독 컴파일·링크 통과 | §4-1 | C002 | ✔ 스텁 1개(직렬 communicate)로 클로저 완성, probe 실행 OK [LOG C006](LOG.md) |
| C007 | 합성 Poisson 생성기 + `driver_pmg.f90` → 첫 수렴, **ref_its 초기 채취** | §4-3 | C006 | ✔ 3종 PASS (its 4/4/5), `run_tests.sh` 가동 [LOG C007](LOG.md) |
| C008 | 덤프 훅 `dump_pmg.f90` (환경변수 게이트) + 본체 재빌드 green | §4-2 | C002 | ✔ setup+pre/post 캡처, 바이트 검증 완료 (r1: 셋업 덤프를 셋업 시점으로 이동) [LOG C008](LOG.md) |
| C009 | 골든 덤프 채취 (rv_model=0 스모크 구간) + **ref 기준 확정** + `run_tests.sh` 완성 | §4-2, §4-4 | C005, C007, C008 | ✔ np=1 s1/s10/s30 — 3중 게이트(충실도·its·bitwise) green. np=2/4 채취는 MPI 하네스 확장(C010 준비)으로 이관 [LOG C009](LOG.md) |
| C010 | **G1**: np=900 실행 가능하게 (파일 방식 유지 최소 수정) — 1차 목표는 **실패 재현·원인 확정** (후보 목록: PLAN §5-1 + 아래 검증 메모) | PLAN §5-1 | C007 | ▶ **합성 기준 달성**: -1 communicate 실물 / -2 np>1 가동 / -3 iar1 오버런(힙 오염) 수정 / -4 **np=900 its=7 green** + 발산 원인 Chebyshev eig 추정으로 확정 [LOG C010-1~4](LOG.md). 잔여: 프로덕션 케이스 np=900 실증(클러스터), eig 추정 강건화 |
| C011 | **G2**: 파일 경유 분배(`MG_tmp/part###.out`)를 MPI 통신으로 대체 — 과도기 파일/통신 이중 모드로 bitwise 일치 확인 후 파일 경로 삭제 | PLAN §5-2 | C010 | ▶ -1 설계·ASCII 라운딩 실험 [LOG C011-1](LOG.md) / -2 `isetup_comm`+PMG_infor BCAST [LOG C011-2](LOG.md) / -3 finest Scatterv + **실버그 2건 수정(y0 힙 가비지 시드 = C009 현상 정체, 솔버 결정화·재베이스라인)** [LOG C011-3](LOG.md). 잔여: part_MG(-4) Scatterv, shim 제거(-5), 파일 경로 삭제(-6) |
| C012 | **G3**: 코드 간소화·가독성 (동작 불변 리팩터링) — its ±0 / bitwise 기준 | PLAN §5-3 | C007 | ▶ 1차 완료: 핵심 3파일 미사용 43건 + 죽은 파일 314줄 제거, 6/6+bitwise green [LOG C012-1](LOG.md). 후속: blaslapack IMPLICIT NONE, 잔여 파일 정리 |
| C013 | 환경 이주 (root→sjdo) — env.sh 경로, poly_smooth 잠재 버그 수정 | — | — | ✔ [LOG C013](LOG.md) |
| C014 | 골든 재채취 — 머신 간 bitwise 재현 확인, 재베이스라인 | §4-2 | C013 | ✔ [LOG C014](LOG.md) |
| C015 | Phase 4 베이스라인 태그(`baseline-phase4`) + perf_log.md 기준표 | §5-4-1 | C014 | ✔ [LOG C015](LOG.md) |
| C016 | 상류 somaFlow.in 교체(ECT1, d5e2177) 호환성 검증 + 신입력 ref_its 재베이스라인 | §4-4 | — | ✔ [LOG C016](LOG.md) |
| C017 | ECT1 신입력 골든 재채취 (`golden/ECT1_436k_np1`) + run_tests 병행 게이트 + 안정성 러닝 | §4-2, §4-4 | C016 | ✔ [LOG C017](LOG.md) |

입력 블로커는 모두 해소됨 (somaFlow.in 제공, rv_model 은 고려하지 않기로 결정 — 2026-08-10, PLAN §7). C009 골든 채취는 rv_model=0 구성의 스모크 구간(step 1~38, 발산 전)에서 수행한다. C012(직렬 리팩터링)는 C007 의 합성 ref_its 만으로 L2 판정이 성립한다. 반면 **C010/C011 은 다중 rank 실행이 필요** — C006/C007 하네스의 MPI 확장으로 소규모 np(2→8→64…)부터 검증하고, np=900 은 로컬 12코어에서 oversubscribe 로 셋업 단계만 재현 시도(소형 합성 격자), 불가 시 np 스케일 외삽 + 산술 검증(파일 수·unit 번호 대역)으로 대체한다.

np=900 실패 원인 검증 메모 (2026-08-09, 소스 정독 기준): 원인 **미확정** — C010 의 1차 목표가 재현·확정이다. 검증으로 **약화된 가설**: ① 유닛 누적에 의한 open-file 한계 — 쓰기·읽기 모두 반복마다 CLOSE 확인(`6_subdomain_infor_mg.f90:188,583`, `2_read_mesh_MPI.f90:205`) ② rank 간 유닛 번호 충돌 — Fortran 유닛은 프로세스-로컬이라 성립 안 함. **남은 후보**: 동일 rank(특히 rank 0) 내 유닛 대역 겹침(PLAN §5-1 "유닛 번호 위생"), 고정 크기 배열 한도(`mxnbne` 등), rank 0 직렬 fan-out 의 부하/시간(hang 으로 보일 수 있음), np≥1000 파일명 인코딩(np=900 과는 무관).
